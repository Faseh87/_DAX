Defining variables:

Profit Negative = 
VAR VProfit = [Total Profit]
VAR VResult = IF(VProfit<0,VProfit,0) 

RETURN
VResult
------------ --------------

Defining variables:

Profit Positive = 
VAR VProfit = [Total Profit]

RETURN 
IF(VProfit>0,VProfit,0) 

------------ --------------

% Monthly Cont_Var = 
    var _TotalSales = CALCULATE([Total Sales Quantity],ALL('Sales Data Folder'))
    RETURN
    DIVIDE([Total Sales Quantity],_TotalSales,"No Sales")
    
